{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "backtest"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
