[project]
name = "core"
version = "0.1.0"
description = "Shared domain types and utilities"
requires-python = ">=3.11"
dependencies = [
    "msgspec>=0.20",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["*.py"]
